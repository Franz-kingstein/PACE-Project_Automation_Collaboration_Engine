# Copy this file to .env in the same folder and fill in values
# Option 1: Direct Qwen (DashScope OpenAI-compatible) in the browser
REACT_APP_QWEN_API_KEY=

# Option 2: Use your own proxy endpoint (recommended)
# e.g. https://your-domain.com/api/chat  (must accept POST { prompt, system })
REACT_APP_CHAT_API_URL=
